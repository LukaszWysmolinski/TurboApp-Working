package com.turbogroup.turboapp.model;

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name = "work_order", schema = "turbodb", catalog = "")
public class WorkOrderEntity {
}
